import { Wallet } from '../wallet';
export declare class EthRegistrar {
    strict: boolean;
    private static assertIsValidLabel;
    private deed;
    private registrar;
    private _wallet;
    constructor(wallet: Wallet, strict?: boolean);
    wallet: Wallet;
    isOwner(label: string): Promise<boolean>;
    isAuctionAvailable(label: string): Promise<boolean>;
    areAllAuctionsAvailable(labels?: string[]): Promise<boolean>;
    startAuctions(labels?: string[]): Promise<string>;
    bid(label: string, chaff?: string[], value?: number, salt?: string): Promise<string>;
    unseal(label: string, value?: number, salt?: string): Promise<string>;
    finalize(label: string): Promise<string>;
    transfer(label: string, newOwner: ArrayBuffer | string): Promise<string>;
    releaseDeed(label: string): Promise<string>;
    private filterOpenAuctions;
}
