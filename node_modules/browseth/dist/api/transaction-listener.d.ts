import { Wallet } from '../wallet';
declare class TransactionListener {
    wallet: Wallet;
    private isPolling;
    static pollInterval: number;
    listeningFor: string[];
    private ee;
    private timer;
    constructor(wallet: Wallet, isPolling?: boolean);
    startPolling(interval?: number): void;
    stopPolling(): void;
    removeAllListeners(): void;
    resolveTransaction(transactionHash: string): Promise<{}>;
    private addTransactionListener;
    private poll;
}
export default TransactionListener;
