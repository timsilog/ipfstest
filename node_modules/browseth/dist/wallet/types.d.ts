import { Rpc } from '../rpc';
export declare abstract class Wallet {
    abstract rpc: Rpc;
    abstract batch: any;
    abstract options: {
        gasPrice: string;
    };
    abstract account(): Promise<string>;
    abstract send(transaction: object): Promise<string>;
    abstract call(transaction: object, block?: string): Promise<string>;
    abstract gas(transaction: object, block?: string): Promise<string>;
    abstract sign(message: string): Promise<string>;
}
