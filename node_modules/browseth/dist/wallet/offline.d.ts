import { Rpc } from '../rpc';
import { Signer } from '../signers';
import { Wallet } from './types';
export declare class Offline implements Wallet {
    rpc: Rpc;
    signer: Signer;
    options: {
        gasPrice: string;
    };
    batch: {
        send: (transaction: any, cb: () => {}) => Promise<((() => {}) | {
            method: string;
            params: string[];
        })[]>;
        call: (transaction: object, block: string, cb: () => {}) => Promise<((() => {}) | {
            method: string;
            params: any[];
        })[]>;
        gas: (transaction: object, block: string, cb: () => {}) => Promise<((() => {}) | {
            method: string;
            params: any[];
        })[]>;
    };
    constructor(rpc: Rpc, signer: Signer, options?: {
        gasPrice: string;
    });
    account: (...args: any[]) => Promise<string>;
    send: (transaction: any) => Promise<any>;
    call: (transaction: object, block?: string | undefined) => Promise<any>;
    gas: (transaction: object, block?: string | undefined) => Promise<any>;
    sign: (message: string) => Promise<string>;
}
