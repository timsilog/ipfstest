import { Rpc } from '../rpc';
import { Online } from './online';
export declare class ReadOnly extends Online {
    address: string;
    constructor(rpc: Rpc, address?: string);
    account: () => Promise<string>;
    send: (transaction: object) => Promise<never>;
    sign: (message: string) => Promise<never>;
}
