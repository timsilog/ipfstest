import * as Transport from '../transport';
import { Default } from './default';
export declare class Whitelist extends Default {
    whitelist: string[];
    constructor(transport: Transport.Handler, whitelist: string[], endpoint?: string, timeout?: number, headers?: {
        [k: string]: string | string[];
    });
    handle(request: Request, cb: (err: Error | void, response?: any) => void): void;
}
